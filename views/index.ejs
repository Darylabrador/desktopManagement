        <% include includes/head %>
    </head>
    <body class="bg-dark d-flex justify-content-center align-items-center" style="height: 100vh !important;">
        <div class="bg-white w-50 mx-auto p-5 rounded">

            <h3 class="text-center"> Bienvenue sur l'espace culturel </h3>

            <% include includes/messageFlash %>

            <form method="POST" action="/login" class="w-75 mx-auto mt-5">
                <div class="form-group">
                    <input required type="email" name="mail" placeholder="ex: john@localhost.com" 
                    class="form-control <%= validationErrors.find(e => e.param === 'mail') ? 'border border-danger' : '' %>" 
                    value="<% if(hasError){%><%=oldInput.mail%><% } %>">
                </div>
            
                <div class="form-group">
                    <input required type="password" name="password" placeholder="*****" 
                    class="form-control <%= validationErrors.find(e => e.param === 'password') ? 'border border-danger' : '' %>">
                </div>
            
                <div class="text-right">
                    <button type="submit" class="btn btn-primary rounded">Se connecter</button>
                </div>
            </form>
        </div>
    </body>
</html>